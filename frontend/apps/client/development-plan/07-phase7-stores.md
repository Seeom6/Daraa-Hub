# 🏪 Phase 7: صفحات المتاجر

## المدة: 3-4 أيام
## الأولوية: 🟡 متوسطة

---

## 📋 الصفحات المطلوبة

### 1. صفحة قائمة المتاجر
**المسار:** `/stores`
**الملف:** `src/app/(main)/stores/page.tsx`

**المتطلبات:**
- عرض كل المتاجر
- فلترة حسب التصنيف
- ترتيب (الأعلى تقييماً، الأحدث)
- بحث بالاسم

### 2. صفحة المتجر
**المسار:** `/stores/[slug]`
**الملف:** `src/app/(main)/stores/[slug]/page.tsx`

**المتطلبات:**
- معلومات المتجر (الشعار، الاسم، الوصف)
- إحصائيات (المنتجات، التقييم، المبيعات)
- منتجات المتجر
- فلترة وترتيب المنتجات
- تقييمات المتجر
- زر المتابعة

### 3. صفحة تقييمات المتجر
**المسار:** `/stores/[slug]/reviews`
**الملف:** `src/app/(main)/stores/[slug]/reviews/page.tsx`

---

## 📁 هيكل الملفات

```
src/
├── app/
│   └── (main)/
│       └── stores/
│           ├── page.tsx                  # قائمة المتاجر
│           └── [slug]/
│               ├── page.tsx              # صفحة المتجر
│               └── reviews/
│                   └── page.tsx          # تقييمات المتجر
├── features/
│   └── stores/
│       ├── components/
│       │   ├── StoreCard.tsx
│       │   ├── StoreGrid.tsx
│       │   ├── StoreHeader.tsx
│       │   ├── StoreInfo.tsx
│       │   ├── StoreStats.tsx
│       │   ├── StoreProducts.tsx
│       │   ├── StoreReviews.tsx
│       │   ├── FollowButton.tsx
│       │   └── StoreFilters.tsx
│       ├── hooks/
│       │   ├── useStores.ts
│       │   ├── useStore.ts
│       │   ├── useStoreProducts.ts
│       │   ├── useStoreReviews.ts
│       │   └── useFollowStore.ts
│       ├── services/
│       │   └── stores.service.ts
│       └── types/
│           └── stores.types.ts
```

---

## 🔧 المكونات المطلوبة

### StoreCard Component
```typescript
interface StoreCardProps {
  store: Store;
  variant?: 'default' | 'compact';
}
// يعرض: الشعار، الاسم، التصنيف، التقييم، عدد المنتجات
```

### StoreHeader Component
```typescript
interface StoreHeaderProps {
  store: Store;
  isFollowing: boolean;
  onFollow: () => void;
}
// بانر المتجر مع الشعار والمعلومات الأساسية
```

### StoreStats Component
```typescript
interface StoreStatsProps {
  productsCount: number;
  rating: number;
  reviewsCount: number;
  followersCount: number;
  salesCount: number;
}
```

### StoreProducts Component
```typescript
interface StoreProductsProps {
  storeId: string;
  initialProducts?: Product[];
}
// قائمة منتجات مع فلترة وترتيب
```

### FollowButton Component
```typescript
interface FollowButtonProps {
  storeId: string;
  isFollowing: boolean;
  followersCount: number;
  onFollow: () => void;
  onUnfollow: () => void;
}
```

---

## 🔗 الـ API Endpoints

| Method | Endpoint | الوصف |
|--------|----------|-------|
| GET | `/stores` | قائمة المتاجر |
| GET | `/stores/:slug` | تفاصيل متجر |
| GET | `/stores/:slug/products` | منتجات المتجر |
| GET | `/stores/:slug/reviews` | تقييمات المتجر |
| POST | `/stores/:id/follow` | متابعة متجر |
| DELETE | `/stores/:id/unfollow` | إلغاء المتابعة |
| GET | `/stores/following` | المتاجر المُتابَعة |

**Query Parameters للـ `/stores`:**
```
?page=1
&limit=20
&category=electronics
&sort=rating_desc
&search=store-name
```

---

## 🎨 تصميم صفحة المتجر

```
┌─────────────────────────────────────────────────────┐
│  ████████████████ COVER IMAGE ████████████████████  │
│                                                     │
│   ┌────┐                                           │
│   │LOGO│  Store Name              [Follow Button]  │
│   └────┘  ⭐ 4.8 (234 reviews) • Electronics       │
│                                                     │
├─────────────────────────────────────────────────────┤
│  📦 150     ⭐ 4.8      👥 1.2K      💰 500+       │
│  Products   Rating     Followers    Sales          │
├─────────────────────────────────────────────────────┤
│                                                     │
│  [All] [Category 1] [Category 2] [Category 3]      │
│                                                     │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐                  │
│  │     │ │     │ │     │ │     │                  │
│  │ P1  │ │ P2  │ │ P3  │ │ P4  │                  │
│  │     │ │     │ │     │ │     │                  │
│  └─────┘ └─────┘ └─────┘ └─────┘                  │
│                                                     │
└─────────────────────────────────────────────────────┘
```

---

## ✅ معايير الإنجاز

- [ ] قائمة المتاجر تعمل
- [ ] صفحة المتجر تعمل
- [ ] منتجات المتجر تظهر
- [ ] الفلترة والترتيب يعملان
- [ ] المتابعة تعمل
- [ ] التقييمات تظهر

---

## ➡️ المرحلة التالية

بعد إكمال هذه المرحلة، انتقل إلى `08-phase8-reviews-wishlist.md`

