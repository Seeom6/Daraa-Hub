# Docker Environment Configuration
# This file is used when running the application in Docker containers

# Server Configuration
NODE_ENV=production
PORT=3001
CLIENT_URL=http://localhost:3000

# Database Configuration
# Use the MongoDB container name as the host
MONGODB_URI=mongodb://daraa-mongodb:27017/daraa-auth

# JWT Configuration
# IMPORTANT: Change these secrets in production!
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-docker
JWT_EXPIRES_IN=7d

# Cookie Configuration
COOKIE_SECRET=your-super-secret-cookie-key-change-this-in-production-docker

# Twilio Configuration (Optional)
# Add your Twilio credentials here for production
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=your_twilio_phone_number

# OTP Configuration
OTP_EXPIRY_MINUTES=5
OTP_MAX_ATTEMPTS=3
OTP_LENGTH=6

